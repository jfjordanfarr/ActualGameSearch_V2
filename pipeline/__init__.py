"""Top-level package marker for the local pipeline workspace.

This enables imports like `from pipeline.scripts import run_etl` in tests/CI
without requiring an editable install of the parent project.
"""
